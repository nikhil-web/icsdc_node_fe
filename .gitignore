node_modules/
node_modules/*
.env
node_modules/.package-lock.json
.env.*
*.log
# Claude Code context file — local only
CLAUDE.md
public/ICSDC_Frontend/sitemap.xml
*.py
sitemap.xml
public/ICSDC_Frontend/sitemap.xml
.claude/launch.json

# Minified output from scripts/build.js — generated, never committed.
# Absent build/ just means the server serves unminified sources.
build/

# Crawler snapshots from prerender.js — generated, never committed.
# Rebuild on the server with `npm run prerender`, or from the admin
# Prerender tab (Build all). Until they exist, bots fall through to the
# normal page, which still carries the server-injected SEO <head>.
public/ICSDC_Frontend/prerendered/
