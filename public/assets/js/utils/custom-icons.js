/**
 * custom-icons.js
 * ───────────────
 * Register your own SVG icons here.
 * Any key defined here takes priority over Font Awesome in resolveIcon().
 *
 * HOW TO ADD AN ICON
 * ──────────────────
 * 1. Add a new entry to CUSTOM_ICONS below:
 *
 *      "your-key": `<svg ...>...</svg>`,
 *
 * 2. Use that key anywhere in Strapi (nav items, page features, etc.)
 *    exactly as you would a Font Awesome name.
 *
 * TIPS
 * ────
 * • Keep SVGs clean — remove width/height attributes and use viewBox only.
 *   Size is controlled by CSS (the icon wrapper sets the dimensions).
 * • Add aria-hidden="true" and focusable="false" to every SVG.
 * • You can paste multi-line SVG strings — template literals handle them fine.
 *
 * EXAMPLE
 * ───────
 *   "icsdc-logo": `<svg viewBox="0 0 24 24" fill="currentColor"
 *                       aria-hidden="true" focusable="false">
 *                    <path d="M12 2L2 7l10 5 10-5-10-5z"/>
 *                    <path d="M2 17l10 5 10-5M2 12l10 5 10-5"/>
 *                  </svg>`,
 */

export const CUSTOM_ICONS = {

    // ── Add your custom icons below ────────────────────────────────────────

    // "my-icon": `<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" focusable="false">
    //               <path d="..."/>
    //             </svg>`,

};
